{
  "name": "gg",
  "type": "module",
  "scripts": {
    "build": "cd astro && npm install && npm run build && cp -r dist ../dist && sed -i '' 's|/gg/|/|g; s|href=\"/gg|href=\"|g; s|src=\"/gg|src=\"|g' ../dist/index.html"
  }
}
